---
# Use the 'create api' subcommand to add watches to this file.
- version: v1alpha1
  group: cache.example.com
  kind: App
  playbook: playbooks/app.yml
- version: v1alpha1
  group: cache.example.com
  kind: Appdb
  playbook: playbooks/appdb.yml
#+kubebuilder:scaffold:watch
